{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "Launch Program",
            "target": "${workspaceFolder}/bin/demo.elf",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
        }
    ]
}